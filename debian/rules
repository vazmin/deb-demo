#!/usr/bin/make -f

# export DESTDIR = $(CURDIR)/debian/deb-demo

%:
	dh $@  

#override_dh_auto_install:
#	dh_auto_install -- prefix=/usr

#override_dh_install:
#	dh_install --list-missing -X.pyc -X.pyo
